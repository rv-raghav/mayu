/**
 * Optional authentication middleware.
 * Re-exports optionalAuth from auth.ts for explicit import by filename.
 * @module middleware/optionalAuth
 */

export { optionalAuth } from '@/middleware/auth';
